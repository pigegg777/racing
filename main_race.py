from RaceSet import Race_Setting
import random


class race:
    def start(time,CarNames) :
        time,CarNames=Race_Setting.set_CarNames(),Race_Setting.set_time()
        score_board={}
        for 
        for _ in range(time):
            for name in CarNames:
                print(name,"",":",end="")
                score=random.randrange(0,10)
                if score>=4:
                    List_score[num]+=1
                for _ in range(List_score[num]):
                    print("-",end="")
                print()
            print()
        return List_score,car_names